Presencha.models.Slide = Ext.regModel("Presencha.models.Slide", {
    fields: [
        {name: "image", type: "string"}
        // order? probably redundant?
        // audio timestamp notes? 
    ]
});
